# 여행 일정표 만들기 — 템플릿 가이드

이 문서는 `2026CANADA`와 **똑같은 방식**으로 다음 여행 일정표를 만들기 위한 안내입니다.
새 여행을 시작할 때 Claude에게 이 파일 링크를 주면 바로 작업을 시작할 수 있습니다.

---

## 1. 이 사이트가 무엇인가

```
2026CANADA/
├── index.html    약 2,800줄  ← HTML + CSS + JS 전부 한 파일
├── sw.js             50줄    ← 오프라인 캐시 (비행기·산속에서도 열림)
├── README.md                 ← 저장소 설명 + 민감정보 원칙
├── TEMPLATE.md               ← 이 문서
└── .nojekyll          0줄    ← GitHub Pages가 파일을 가공하지 않게 함
```

**설계 원칙 — 이게 핵심입니다.**

| 원칙 | 이유 |
|---|---|
| **한 파일** | 빌드 도구 없음. 푸시하면 1분 뒤 배포 끝 |
| **외부 라이브러리 0개** | CDN이 죽으면 여행지에서 사이트가 안 열림 (실제로 겪음) |
| **API 키 0개** | Public 저장소라 키를 넣을 수 없음 |
| **모바일 우선** | 노트북이 아니라 폰으로 보는 물건 |
| **오프라인 동작** | 모레인레이크는 셀 신호가 아예 없음 |

외부 통신은 딱 두 군데뿐입니다.
- `api.open-meteo.com` — 날씨 (키 불필요)
- `tile.openstreetmap.org` — 지도 타일 (키 불필요)

둘 다 끊겨도 사이트 본문은 정상 동작합니다.

---

## 2. 새 여행 시작하기 — 3단계

### 1단계 · GitHub 저장소 만들기

1. GitHub → **New repository**
2. 이름: 여행 식별자 (예: `2027JAPAN`, `2027JEJU`)
3. **Public** 선택 — Pages를 무료로 쓰려면 필요
4. 만든 뒤 → **Settings → Pages → Source: `main` / `/ (root)`**
5. 몇 분 뒤 `https://0818soo-wq.github.io/2027JAPAN/` 이 살아납니다

> 저장소를 만든 다음 Claude에게 **"이 저장소 붙여줘"** 라고 하면 작업을 시작할 수 있습니다.

### 2단계 · 시작 프롬프트 (아래를 복사해서 채우기)

```
[새 저장소]  github.com/0818soo-wq/2027JAPAN
[참고]       github.com/0818soo-wq/2026CANADA 와 같은 형식으로

[기본정보]
기간:   2027-04-03 ~ 2027-04-11 (9일)
인원:   어른 2 + 지우 (여행 시점 만 O세)
도시:   도쿄 → 하코네 → 오사카
이동:   시내 대중교통 / 도시간 신칸센 / 렌터카 여부
예산:   대략 O만원

[이미 확정된 것]        ← 있으면 반드시 먼저 주세요
항공:   4/3 OZ102 ICN 09:00 → NRT 11:20
        4/11 OZ103 KIX 13:00 → ICN 15:30
숙소:   4/3~4/6 신주쿠 힐튼 (예약완료, 포인트)
렌터카: 없음

[꼭 하고 싶은 것]
지브리미술관 / 디즈니씨 / 하코네 온천 1박

[피하고 싶은 것]
하루 3곳 이상 이동, 아이 못 들어가는 곳

[규칙]
- 민감정보(여권번호·eTA/ESTA번호·카드번호·집주소)는 github나 웹페이지에 올리지 말 것
- 예약번호는 앞 3~4자리만 표시
- 매번 자동배포 (main 브랜치)
- 사이트에 추가하기 전에 먼저 나에게 설명하고 승인받기
- 나한테 말할 때는 한국말로
```

**[규칙] 5줄이 가장 중요합니다.** 지난번엔 작업 중간중간 알려주셨는데, 처음부터 있으면 세션 내내 지켜집니다.

**[이미 확정된 것]** 도 중요합니다. 항공편·숙소가 정해져 있으면 그 시각을 기준으로 앞뒤를 짜기 때문에 되묻는 횟수가 확 줄어듭니다.

### 3단계 · 진행 방식

지난번에 잘 통했던 흐름입니다.

```
조사 → Claude가 설명 → 내가 승인/수정 → 반영 → 배포 → 다음 항목
```

특히 효과 좋았던 것:
- **스크린샷 붙여넣기** — 호텔 방 사진, 예약 화면, 호텔에서 온 이메일.
  시애틀 숙소는 이 방식으로 결정했습니다.
- **"이건 빼"** / **"이건 삭제"** — 짧게 말해도 됩니다.
- **"이상하지 않아?"** — 동선이 어색하면 그냥 물어보세요. 실제로 Day 6 순서가 그렇게 고쳐졌습니다.

---

## 3. index.html 구조 — 어디를 고치면 되는가

### 탭 5개

| 탭 | id | 내용 |
|---|---|---|
| 일정 | `#tab-itinerary` | 일자별 카드 (HTML 직접 작성) |
| 동선 | `#tab-route` | `dayRouteData` 배열 → 지도 자동 생성 |
| 식사 | `#tab-meals` | `MEAL_DAYS` + `mealData` |
| 비용 | `#tab-budget` | `.budget-day` / `.cost-row` HTML |
| 예약사항 | `#tab-booking` | `.booking-card` HTML |

### 일자 카드 (일정 탭)

```html
<div class="day seg-van" data-date="2026-09-16">
  <span class="day-dot"></span>
  <div class="card">
    <div class="card-head">
      <div class="card-head-left">
        <span class="day-num">Day 01</span>
        <span class="day-date">9.16 (수)</span>
      </div>
      <span class="transport-badge">✈ 항공 KE075</span>
    </div>

    <div class="weather" data-weather="partly-cloudy">
      <span class="weather-icon">⛅</span>
      <span class="temp">22–23° / 13–14°</span>
      <span class="outfit">얇은 긴팔 + 가디건</span>
    </div>

    <div class="stay">
      <a class="stay-name" href="google maps 링크">Hilton Vancouver Downtown</a> 체크인
    </div>

    <ul class="schedule">
      <li>18:44 공항버스 → 19:33 인천공항 T2</li>
      <li class="flight">22:40 ICN 출발 <span class="tag">KE075</span></li>
      <li class="drive">렌터카 약 1시간</li>
      <li class="note">ⓘ 참고사항</li>
      <li class="warn">⚠ 주의사항</li>
    </ul>
  </div>
</div>
```

`<li>` 클래스 4종이 색과 아이콘을 결정합니다.

| 클래스 | 쓰임 |
|---|---|
| (없음) | 평범한 일정 |
| `flight` | 항공편 |
| `drive` | 차량 이동 |
| `note` | 참고 (회색) |
| `warn` | 주의 (빨강) — **놓치면 여행이 꼬이는 것에만** |

`data-date="YYYY-MM-DD"` 는 필수입니다. 오늘 날짜 자동 하이라이트와 실시간 날씨가 이 값을 씁니다.

### 지역 구분 (`seg-*`)

지역마다 색과 날씨 좌표가 따라옵니다. 새 여행에선 지역명에 맞춰 바꿉니다.

```js
const segCoords = {
  'seg-van':   { lat: 49.2827, lon: -123.1207 },
  'seg-banff': { lat: 51.0884, lon: -115.3481 },
  'seg-cal':   { lat: 51.0447, lon: -114.0719 },
  'seg-sea':   { lat: 47.6062, lon: -122.3321 }
};
```

### 동선 지도 — 데이터 2개만 채우면 끝

**① `GEO` — 지명을 위경도로**

```js
const GEO = {
  'Vancouver International Airport' : [49.1967, -123.1815],
  '433 Robson St, Vancouver, BC'    : [49.2795, -123.1173],
};
```

**② `dayRouteData` — 일자별 경유지**

```js
{ day:'Day 02', date:'9.17(목)', seg:'seg-van',
  stops:['호텔','스탠리공원','그랜빌아일랜드','호텔 (복귀)'],
  connectors:[
    {icon:'transit', text:'버스 19번 · 약 10분'},
    {icon:'transit', text:'씨월 도보 · 약 25분'},
    {icon:'transit', text:'Aquabus · 약 5~10분'}
  ],
  map: mapFrame([...GEO 키들...], { mode:'transit' })
}
```

**⚠ 반드시 맞춰야 하는 것**
- `stops` 개수 = 지도 핀 개수 = `mapFrame()` 에 넣은 좌표 개수
- `connectors` 개수 = `stops` 개수 − 1

지난번에 Day 02·03·07에서 이게 어긋나 번호와 설명이 밀렸습니다. 번호를 붙이기 직전에 검사해서 잡았는데, **새로 만들 때도 꼭 검사해야 합니다.**

`icon` 값: `car` / `transit` / `plane` / `walk`
`mapFrame` 옵션:
- `{ mode:'transit' }` — 구글 길찾기를 대중교통으로 (기본은 운전)
- `{ dir:[...] }` — 길찾기 링크만 다른 구간으로 (예: 비행 구간 제외)
- `{ dir:null }` — 길찾기 링크 없음 (귀국일 등)

### 지도 엔진 — 손대지 말 것

`lon2px` / `lat2px` / `fitMap` / `paintMap` / `buildRouteMap` / `openMapFull` / `initRouteMaps`

Web Mercator 타일 계산을 직접 구현한 부분입니다. **여행이 바뀌어도 그대로 복사**하면 됩니다.

> **왜 직접 짰나:** 처음엔 구글 지도 iframe을 썼는데 `?q=A|B|C` 는 길찾기가 아니라 *검색*이라 선이 안 그려졌습니다. Leaflet으로 바꿨더니 실제 기기에서 CDN이 막혀 지도가 통째로 안 떴고, 대비책으로 넣은 구글 지도는 이제 API 키를 요구했습니다. 결국 **외부 의존성을 전부 없애는 것**이 유일한 해법이었습니다.

### 식사 탭

```js
const MEAL_DAYS = [
  { k:'day1', n:'Day 01', t:'9.16 (수) · 밴쿠버', seg:'seg-van' },
];
// mealData.day1 = { 아침:…, 점심:…, 저녁:… }
// 값: 문자열 | {name, place, desc} | [{...}, {...}]  (place 있으면 구글맵 링크)
```

### 비용 탭

```html
<div class="cost-row">
  <div class="cost-label">대한항공<small>KE075 · 왕복</small></div>
  <div class="cost-amount paid">₩4,174,000</div>
</div>
```

`paid` = 파랑(지출완료) / `planned` = 빨강(지출예정).
상단 총액 3개(총액·지출완료·지출예정)는 **수기 입력**이므로 항목을 고칠 때마다 같이 고쳐야 합니다.

### 짐·쇼핑 체크리스트

`packData` / `shopData` 배열로 항목을 정의합니다.
체크 상태는 `localStorage` 에 저장되므로 **여행마다 키를 반드시 바꿔야** 이전 여행 체크가 딸려오지 않습니다.

```js
const PACK_KEY = 'pack2026nw';   // → 'pack2027jp'
const SHOP_KEY = 'shop2026nw';   // → 'shop2027jp'
```

### 서비스워커 캐시 버전

내용을 고친 뒤에는 `sw.js` 의 버전을 올려야 이미 방문한 기기에서 갱신됩니다.

```js
var CACHE = 'nw2026-v4';   // 고칠 때마다 v5, v6 …
```

새 여행이면 접두사도 바꿉니다 (`jp2027-v1`).

---

## 4. 배포

```bash
git add -A
git commit -m "설명"
git push -u origin main
```

GitHub Pages가 1분 내에 반영합니다. 빌드 과정이 없습니다.

Claude와 작업할 때는 작업 브랜치에서 작업하고 `main` 으로 머지하는 방식을 씁니다.

---

## 5. 민감정보 원칙 — 이건 매번 지킬 것

저장소가 **Public** 이므로 링크를 아는 사람은 누구나 봅니다.

**절대 올리지 않는 것**
- 여권번호 · 여권 사진 · 생년월일
- eTA / ESTA / 비자 신청번호
  → 신청번호 + 여권번호 + 생년월일이면 제3자가 승인 정보를 조회·변경할 수 있습니다
- 카드번호 등 결제수단
- 집 주소
- API 키

**예약번호는 앞 3~4자리만.**
성(姓) + 예약번호 조합이면 예약 조회·변경·**취소**까지 가능합니다.

탑승객 이름·좌석·숙소 주소는 여행에 필요해서 그대로 두되, **링크를 외부에 공유하지 마세요.**

---

## 6. 지난 여행에서 실제로 걸렸던 함정들

새 여행에서도 같은 종류의 실수가 나옵니다. 조사할 때 이 목록을 확인하세요.

**교통·법규**
- **차량 공유 앱은 카시트 면제가 안 됩니다.** 워싱턴주(RCW 46.61.687) 기준 택시만 면제고 Uber/Lyft는 아닙니다. 아이와 함께면 **택시 / 대중교통 / 내 카시트** 중에서 골라야 합니다. → 목적지 지역의 카시트 법을 매번 확인
- **계절 운행 종료일.** 시애틀 워터프론트 셔틀이 9월 7일에 운행을 끝내는 걸 모르고 넣을 뻔했습니다. 관광 교통편은 **운행 기간**을 꼭 확인
- **도로 폐쇄.** 밴프 Bow Valley Parkway 동쪽 구간이 9/1~10/6 폐쇄라 존스턴 캐년을 Castle Junction으로 돌아가야 했습니다. **내비 앱에 안 나올 수 있어** 사이트에 `warn` 으로 따로 적었습니다

**예약**
- **포인트는 1박 기준인지 총액인지 확인.** 힐튼 화면의 "14만 포인트"가 *1박 평균*이라 실제로는 28만이 필요했습니다. 이걸 놓쳐서 숙소 추천이 한 번 뒤집혔습니다
- **인기 명소는 예약 방식이 바뀝니다.** 스타벅스 1호점이 2026년 6월 재개장하며 줄서기를 없애고 현장 예약제로 바뀌었습니다 (온라인 불가). **최근 정보인지** 확인
- **셔틀·입장권 티켓 오픈 시각.** 모레인레이크는 여행 중에 현지 시각 기준으로 잡아야 합니다

**아이 동반**
- 입장권은 **나이 기준**을 확인 (예: 3세부터 티켓 필요)
- 하이킹은 **유모차 가능 여부**와 왕복 소요시간
- 하루 일정에 **빠질 수 있는 항목**을 하나 넣어두기 (지우가 지치면 생략)

**기술**
- **외부 CDN·API를 새로 넣지 말 것.** 여행지 네트워크에서 막히면 그때는 고칠 수 없습니다
- 시각을 적을 때 **현지 시각인지 한국 시각인지** 명시. 알람 하나를 12시간 착각한 적이 있습니다

---

## 7. 사이트 말고 따로 만들었던 것들

여행 준비에 같이 만들면 좋은 것들입니다. 필요하면 요청하세요.

- **아이용 그림 일정표** — 글자를 못 읽는 아이를 위해 그림만으로 만든 주간 플래너
- **그림책 책자** — 위를 A4 양면 인쇄용으로 배치한 PDF (중철 제본 + 낱장 두 가지)
- **인쇄물 체크리스트** — 출발 전 뽑아야 할 것 목록
- **숙소·이메일 답장 초안**

이것들은 저장소에 올리지 않고 파일로 받았습니다 (이름·나이가 들어가므로).

---

## 8. 한 줄 요약

> 새 저장소 만들고 → 위 **시작 프롬프트**를 채워서 주고 → 조사·설명·승인을 반복하면 됩니다.
> `index.html` 한 파일에 전부 들어가고, 외부 의존성은 날씨와 지도 타일 둘뿐입니다.
