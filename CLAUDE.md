# 작업 규칙

## 최우선 — 사실과 일관성

**모든 상황에 적용됩니다. 다른 어떤 규칙보다 우선합니다.**

### 지어내지 않기

- **확인하지 않은 수치·시각·요금·규정을 말하지 않습니다.** 검색으로 확인하거나,
  못 찾으면 **"확인하지 못했다"고 그대로 말합니다.**
- 답할 때 세 가지를 구분해서 씁니다.
  - **확인함** — 출처를 함께 답니다
  - **추정** — "추정"이라고 명시하고 근거를 밝힙니다
  - **모름** — 모른다고 말하고, 사용자가 확인할 방법을 알려줍니다
- 다른 지역·다른 업체의 수치를 가져다 쓰지 않습니다.
  (예: 캘거리 공항 요금을 밴쿠버 공항 요금으로 제시 ✗)
- "아마", "보통", "대개"로 넘어가지 않습니다. 근거가 없으면 없다고 씁니다.

### 일관성 지키기

- **한 번 정한 기준·순위·권고를 뒤집지 않습니다.**
- 뒤집어야 할 새 근거가 생기면 **뒤집는다는 사실과 이유를 먼저 밝히고** 바꿉니다.
- 우선순위를 제시했으면 그 순위를 끝까지 유지합니다.
  예외 조건은 "1~3순위가 모두 불가할 때"처럼 **조건을 명시**해 덧붙입니다.
- 앞서 한 말과 충돌하는 답을 쓰기 전에 앞 내용을 먼저 확인합니다.

### 틀렸을 때

- 짧게 사실만 정정하고 넘어갑니다. 변명하거나 길게 자책하지 않습니다.


## 대기 중인 작업 — 사용자 신호를 기다림

### 셔틀 예약 성공 신호 시 (사용자가 "성공했다"고 알려주면)

`모레인 예약 팁` 팝업(`id="tipModal"`, 버튼 라벨 `모레인 예약 팁`)을
**`모레인 상세동선`** 으로 **통째로 교체**합니다.

- 내용: 9/20 시간별 동선 **+ 구간별 운전시간**
- 기준안: **레이크루이스 7–8am 티켓 · 에메랄드 카누 + 보우호 + 페이토호 전부**
  (05:30 기상 → 06:15 캔모어 출발 → 07:15 체크인 → 07:40 레이크루이스
   → 09:15 Connector → 09:45 모레인 → 11:30 복귀 → 12:15 Bill Peyto's
   → 13:00 서쪽 → 13:45 카누 → 15:00 북쪽 → 16:10 보우호 → 16:55 페이토
   → 17:30 출발 → 19:10 캔모어)
- 운전: 캔모어→P&R 81km·58분 / P&R→빌리지 약 7분 / 빌리지→에메랄드 35km·35분
  / 에메랄드→보우호 약 75km·70분 / 보우호→페이토 5~7km·5분
  / 페이토→캔모어 약 126km·1시간 40분 · **총 4시간 35분**
- 티켓이 모레인이면 순서만 모레인 → 레이크루이스로 뒤집기
- 함께: `dayRouteData`의 Day 05 `stops`에 보우호·페이토호 추가
  (**`connectors` = `stops` − 1** 지킬 것)

## 진행 중 — 2026 북미 여행 (9/16~9/26)

여행 중입니다. 사용자가 실제 지출을 알려주면 **되묻지 말고 즉시 반영하고 배포**합니다.

### 지출 반영 절차

1. 해당 일자의 `cost-row`를 찾아 `planned` → `paid`로 바꾸고 금액을 실지출로 교체
   - 예산에 없던 새 항목이면 그 일자에 행을 추가
2. `<small>`에 원화폐 금액을 `<b>CA$00.00 실지출</b>` 형태로 표시
3. 상단 3개 합계(총액 / 지출완료 / 지출예정)를 개별 항목 합산으로 재계산
4. `sw.js`의 `CACHE` 버전 +1
5. 작업 브랜치 커밋 → `main` 머지 → 푸시
6. 답변에 갱신된 3개 합계와, 예상 대비 차이가 크면 그 사실을 적음

### 환율

- CA$1 ≈ ₩990 · US$1 ≈ ₩1,390
- 카드 명세서가 나오면 실제 환율로 보정 (사용자가 요청할 때)

## 상시 규칙

- **민감정보는 GitHub·웹페이지에 올리지 않음** — 여권번호, eTA/ESTA 신청번호,
  카드번호, 집주소, API 키. 예약번호는 앞 3~4자리만.
- **매번 자동배포** — 작업 브랜치 커밋 후 `main`에 머지·푸시
- **사이트에 추가하기 전에 먼저 설명하고 승인받기** (지출 반영은 예외 — 즉시 반영)
- **사용자에게는 한국말로 답변**
- **지우(홍지우)는 만 4세 남아** — 딸이 아니라 아들

## 구조

`index.html` 한 파일에 전부 들어 있습니다. 자세한 내용은 [TEMPLATE.md](TEMPLATE.md) 참고.

- 비용 탭: `id="tab-budget"` · `.budget-day` > `.cost-row` > `.cost-amount.paid|.planned`
- 일자 카드: `data-date="YYYY-MM-DD"`
- 동선: `GEO` 좌표 사전 + `dayRouteData` 배열
  - **`stops` 개수 = `mapFrame()` 좌표 개수, `connectors` = `stops` − 1**

### 카드 앱 시간 주의

토스/트래블GO 이용내역은 **한국 시간**으로 날짜를 묶습니다.
밴쿠버·밴프는 −16시간, 시애틀은 −16시간이므로
**한국 날짜 기준 오전~낮 거래는 전날 현지 저녁**일 수 있습니다.
어느 일자에 넣을지 애매하면 사용자에게 확인하세요.
